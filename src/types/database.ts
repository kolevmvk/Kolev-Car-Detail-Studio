// Placeholder — will be replaced by `supabase gen types` once schema migrations run.
// Using a permissive type here avoids blocking Phase 1 UI work.
export type Database = {
  public: {
    Tables: Record<string, never>;
    Views: Record<string, never>;
    Functions: Record<string, never>;
    Enums: Record<string, never>;
  };
};
